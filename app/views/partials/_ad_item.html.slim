.col-3
  .card
    - if item.images.attached?
      = image_tag item.images.first.variant(resize: "600"), class: 'img-thumbnail', style: 'margin: 4px'
    - else
      = image_tag 'no_img.png', class: 'img-thumbnail', alt: 'no image available',  style: 'margin: 4px'
    .card-body
      = link_to "#{item.category.name}", category_path(item.category), class: "grey-small-text", style: "border: 1px solid #999; padding: 4px; border-radius: 4px"
      hr
      h5.grey-small-text = "AD number: #{item.id}"
      p.created-at-date
        = item.created_at.strftime('%Y-%m-%d %H:%M:%S')
        br
      h5.card-title
        = link_to "#{item.name}", ad_path(item)
      p.card-text
        = item.description
  br